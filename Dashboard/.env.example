# Base URL of the FastAPI gateway. The WebSocket URL is derived from this
# automatically (http -> ws), so there is only one thing to change if the
# gateway moves to another host or port.
#
# Optional: the dashboard falls back to http://localhost:8000 when unset,
# which is correct for the standard local demo setup.
VITE_GATEWAY_URL=http://localhost:8000
